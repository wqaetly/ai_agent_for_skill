# REQ-17 任务2.2 ActionRecommender

## 工作范围
- 基于上下文的Action推荐引擎，负责过滤、组合验证与解释输出。

## 实施步骤
1. 解析skill_context并映射到需求标签（输出/控制/位移等）。
2. 应用REQ01中的约束矩阵，剔除冲突Action。
3. 综合语义分、频率分、模式匹配得出排序与置信度。
4. 输出推荐理由、兼容Action、注意事项供Tool3使用。

## 依赖
- RAG召回、Action统计、模式分析（可选）、规则库。

## 验收标准
- 至少返回1组可行组合；结果包含reason。
- 代码含联调测试，验证不同上下文场景。

## 风险
- 上下文不充分 → 提供默认假设并提示用户补充。

## 里程碑
- 预计2天完成核心算法与测试。

## 交付物
- action_recommender.py、样例prompt、A/B评估脚本。
