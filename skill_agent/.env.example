# SkillRAG 环境变量配置示例
# 将此文件复制为 .env 并填写实际值

# ==================== 必需配置 ====================

# DeepSeek API 密钥（必需）
# 用于技能生成、验证修复等LLM调用
# 获取地址：https://platform.deepseek.com/
DEEPSEEK_API_KEY=your_deepseek_api_key_here

# ==================== LangGraph 服务器配置 ====================

# 服务器监听地址（默认：0.0.0.0，监听所有网络接口）
LANGGRAPH_HOST=0.0.0.0

# 服务器端口（默认：2024）
LANGGRAPH_PORT=2024

# ==================== 可选配置 ====================

# DashScope API 密钥（可选）
# 用于阿里云Qwen Embedding（如使用本地模型可不配置）
# 获取地址：https://dashscope.console.aliyun.com/
# DASHSCOPE_API_KEY=your_dashscope_api_key_here

# OpenAI API 密钥（可选）
# 备用LLM提供商，可在config.yaml中切换
# OPENAI_API_KEY=your_openai_api_key_here

# Claude API 密钥（可选）
# 备用LLM提供商，可在config.yaml中切换
# CLAUDE_API_KEY=your_claude_api_key_here

# LangSmith API 密钥（可选）
# 用于LangChain调试和追踪，生产环境可选
# 获取地址：https://smith.langchain.com/
# LANGSMITH_API_KEY=your_langsmith_api_key_here

# LangSmith 项目名称（可选）
# LANGSMITH_PROJECT=skill-rag

# LangSmith 追踪开关（可选）
# LANGSMITH_TRACING=true

# ==================== Unity RPC 服务器配置 ====================

# Unity RPC 服务器端口（默认：8766）
# UNITY_RPC_PORT=8766

# Unity RPC 服务器地址（默认：0.0.0.0）
# UNITY_RPC_HOST=0.0.0.0

# ==================== 日志配置 ====================

# 日志级别（可选值：DEBUG, INFO, WARNING, ERROR, CRITICAL）
# LOG_LEVEL=INFO

# 日志文件路径（可选）
# LOG_FILE=logs/skillrag.log

# ==================== RAG 配置 ====================

# 向量数据库路径（可选，默认使用config.yaml中的配置）
# VECTOR_DB_PATH=Data/vector_db

# 嵌入模型路径（可选，默认使用config.yaml中的配置）
# EMBEDDING_MODEL_PATH=Data/Models/Qwen3-Embedding-0.6B

# ==================== 使用说明 ====================

# 1. 最小化配置（仅本地开发）：
#    只需配置 DEEPSEEK_API_KEY 即可启动

# 2. 生产环境配置：
#    - 必需：DEEPSEEK_API_KEY
#    - 推荐：LANGSMITH_API_KEY（用于监控和调试）
#    - 可选：根据实际需求配置其他LLM提供商

# 3. 安全提示：
#    - 不要将 .env 文件提交到 Git 仓库
#    - .gitignore 已包含 .env，请勿修改
#    - 生产环境建议使用密钥管理服务（如 AWS Secrets Manager）

# 4. 配置优先级：
#    环境变量 > .env 文件 > config.yaml 默认值
